{% extends 'testing.html.twig' %}



{% block content %}

    <div class="container">

    <br/>
    <br/>
    <br/>
    <br/>
    <br/>

                <div class="row">

        <div class="col-6 col-md-4">
         {% for message in app.flashes('danger')%}
          <div id="hideDiv" class="alert alert-success">
            {{message}}
             </div>
           {% endfor %}

        
             </div>

    <div class="col-6 col-md-4">
                <h4>Mot de passe oublié</h1>
                <br/>

      <form method="post">
                                        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}

                    <div class="input-group col-lg-12 mb-4">
                        <div class="input-group-prepend">
                            <span class="input-group-text bg-white px-4 border-md border-right-0">
                                <i class="fa fa-envelope text-muted"></i>
                            </span>
                        </div>
                                  {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
                                  {{ form_widget(form.email,{'attr':{'placeholder':'Email Address','class':'form-control bg-white border-left-0 border-md' }})}}
                    </div>

              


                    <div class="form-group col-lg-12 mx-auto mb-0">
                        <button type="submit" class="btn btn-primary btn-block py-2">
                            <span class="font-weight-bold">Valider</span>
                        </button>
                    </div>

                  <input type="hidden" name="_csrf_token"  value="{{ csrf_token('authenticate') }}" />



                                    {{ form_end(form) }}        

            </form>



               
            </div>
        </div>
        </div>


        <style>
/*
*
* ==========================================
* CUSTOM UTIL CLASSES
* ==========================================
*
*/

.border-md {
    border-width: 2px;
}


/*
*
* ==========================================
* FOR DEMO PURPOSES
* ==========================================
*
*/

body {
    min-height: 100vh;
}

.form-control:not(select) {
    padding: 1.5rem 0.5rem;
}

select.form-control {
    height: 52px;
    padding-left: 0.5rem;
}

.form-control::placeholder {
    color: #ccc;
    font-weight: bold;
    font-size: 0.9rem;
}
.form-control:focus {
    box-shadow: none;
}



</style>


{% endblock %}
